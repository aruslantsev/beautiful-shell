

if [ ${_BS_SIMPLE_PROMPT:-0} -eq 1 ]; then
	case "${_BS_SHELL_NAME}" in
		"bash" | "ash" | "sh")
			. ${_BS_PATH_PREFIX}/scripts/ps1-simple-sh
			;;
		"zsh")
			. ${_BS_PATH_PREFIX}/scripts/ps1-simple-zsh
			;;
	esac

else
	. ${_BS_PATH_PREFIX}/scripts/ps1-multiline
fi
